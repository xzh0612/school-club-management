server:
  port: 8080

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.club.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: ${JWT_SECRET:club-management-secret-key-2026-spring-boot-vue}
  expiration: 86400000

logging:
  level:
    com.club.mapper: debug

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/School-Club-Management-System?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Xzh060612
    driver-class-name: com.mysql.cj.jdbc.Driver
